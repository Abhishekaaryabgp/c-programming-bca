#include <stdio.h>
int main()
{
    int r = 7;
    int h = 28;
    float pi = 3.14;

    printf("\n the radious of circle is = %f", pi * r * r);

    printf("\n Volume of cylender is = %f", pi * r * r * h);

    return 0;
}
