#include <stdio.h>
int main()
{
   char st[30];
   printf("Enter your name :...");
   gets(st);//to input.
   puts(st);//to output.
   printf("Your name is|| %s || Nice name :",st);
    return 0;
}
