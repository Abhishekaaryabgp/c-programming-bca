#include <stdio.h>
int main()
{
    int x, y, z, k, result;
    printf("\n Sir enter the values for these constant`s...");
    scanf("%d %d %d ", &x, &y, &z);
    result = 3 * x / y - z + k;
    printf("\n Your answer is %d", result);
    return 0;
}
