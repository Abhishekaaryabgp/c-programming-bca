#include <stdio.h>
#include <conio.h>
int main()
{
    int n, i;
    printf("\n Enter the integer number :");
    scanf("%d", &n);
    for (i = 1, i; 1 <= 10; ++i)
    {
        printf("%d * %d = %d",n,i,n * 1);
    }
    return 0;
}
