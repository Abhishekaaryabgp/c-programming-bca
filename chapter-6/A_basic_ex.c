#include <stdio.h>
int main()
{
    // int  var1;
    //char var2;
    // printf(" Address of var1 is %d\n",&var1);//This line shows that the address of variable1 is storing//
    // printf(" Address of var2 is %c\n",&var2);//





    int  a,b,c;
    printf("\n Enter the value of c");
    scanf("%u %u",&a,&b);
    c = a+b;
    printf("The output of addition is %u",c);

    return 0;
}
