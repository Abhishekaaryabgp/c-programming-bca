#include <stdio.h>
int main()
{
    int a;
    printf("Enter the value for a ,b ,and c...\n");
    scanf("%d ",&a);

    (a > 99.9) ? printf("A is bigger than 99.9\n"): printf("A is less than than 99.9\n");
    
    return 0;
}
