#include <stdio.h>
int main()
{
    int x;
    printf("\n Enter a num for x...");
    scanf("%d", &x);
    //if conditioin is true//                   //if  condition is false//
    (x >= 10) ? printf("\n x is greater than 10") : printf("\n x is less than 10");
    return 0;
}
