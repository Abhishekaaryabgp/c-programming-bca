#include <stdio.h>
#include <conio.h>  
int main()
{
    int marks[5], sum; // this is for interger ...
    marks[0] = 70;
    marks[1] = 60;
    marks[2] = 65;
    marks[3] = 59;
    sum = marks[0] + marks[1] + marks[2] + marks[3];
    printf("the total sum  is %d\n", sum);

    return 0;
}
